# Default values for trocar.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: ghcr.io/jpmaca/trocar
  pullPolicy: Always
  # Overrides the image tag whose default is the chart appVersion.
  tag: "20220511145314"

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""
auth0:
  audience: "https://trocar.auth0.com/api/v2/"
  domain: "trocar.auth0.com"